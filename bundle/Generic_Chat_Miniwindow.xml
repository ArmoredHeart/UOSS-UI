<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE muclient>

<!-- Bits of this plugin and ideas were borrowed and remixed from the MUSHclient community. http://www.gammon.com.au/forum/?id=9385 and others. -->
<!-- Modifications for Aardwolf and extra awesome sauce added by Fiendish with help from Orogan -->
<!-- Adapated by Nick Gammon for Smaug and similar MUDs -->
<!-- Mousewheel scroll support added -->

<muclient>
    <plugin
    name="Generic_Chat_Miniwindow"
    author="Fiendish"
    id="10778718503f0c91106745f5"
    language="Lua"
    purpose="Moves chats to a themed miniwindow while preserving original chat colours"
    date_written="2010-04-04"
    date_modified="2011-01-05"
    requires="4.55"
    version="2.8"
    save_state="y"
    >

<description trim="y">
USAGE:

  chats echo on    : echo chats in main window
  chats echo off   : do not echo chats
  chats echo       : toggle echoing
  chats timestamps on  : show timestamps
  chats timestamps off : do not show timestamps
  chats timestamps     : toggle timestamps
  chats show       : show chats window
  chats hide       : hide chats window

  chats sapi next  : read next unread line

  LH-click a line to copy it to the clipboard
  RH-click main window to see menu of options
  Mouse wheel over the chat area to scroll

  Click title bar to drag window.

  Call this plugin by making as many triggers as you want in your main
  world file (or other plugins) and then passing the style runs to this
  plugin like this. The standard display() API preserves the user's echo
  preference; display_noecho() always writes only to the miniwindow and is
  used by SquareSoft_Chat_Router so caught chat never duplicates in main output:

  function chattrigger (name, line, wildcards, styles)
    require "serialize"
    check (CallPlugin ("10778718503f0c91106745f5",
                       "display", serialize.save_simple (styles)))
  end -- chattrigger


Modular theme client: colours are supplied by UI_Theme_Controller v2.0 when available.
</description>

</plugin>

<aliases>
    <alias
    script="chat_echo"
    match="^chats echo( on| off)?$"
    enabled="y"
    regexp="y"
    sequence="100"
    ignore_case="y"
    ></alias>

    <alias
    script="chat_sapi"
    match="^chats sapi next$"
    enabled="y"
    regexp="y"
    sequence="100"
    ignore_case="y"
    ></alias>

    <alias
    script="chat_timestamps"
    match="^chats timestamps( on| off)?$"
    enabled="y"
    regexp="y"
    sequence="100"
    ignore_case="y"
    ></alias>

    <alias
    script="chat_show"
    match="chats show"
    enabled="y"
    sequence="100"
    ignore_case="y"
    ></alias>

    <alias
    script="chat_hide"
    match="chats hide"
    enabled="y"
    sequence="100"
    ignore_case="y"
    ></alias>

</aliases>

<script>
<![CDATA[

require "movewindow"
require "copytable"

BODY_FONT_NAME = GetVariable("bodyfont") or "Lucida Console"
BODY_FONT_SIZE = tonumber(GetVariable("font_size")) or 11
BASE_BODY_FONT_SIZE = 11
BASE_TITLE_FONT_SIZE = 10
SCROLL_BAR_WIDTH = 15
MAX_LINES = 10000

TIMESTAMP_TEXT_COLOUR = "white"
TIMESTAMP_BACK_COLOUR = "black"

timestamp = not (GetVariable("timestamp") == "false")
echo = not (GetVariable("echo") == "false")

date_format = GetVariable("date_format")
WINDOW_WIDTH = tonumber(GetVariable("WINDOW_WIDTH"))
WINDOW_HEIGHT = tonumber(GetVariable("WINDOW_HEIGHT"))

TEXT_INSET = 5

lines = {}
rawlines = {}

lineStart = ""
lineEnd = ""
WINDOW_COLUMNS = ""
WINDOW_LINES = ""

THEME_CONTROLLER_ID =
    "43c8d047be7a418ba5e86916"

THEME_BROADCAST_MESSAGE = 9042


--------------------------------------------------
-- MODULAR THEME CLIENT
--
-- The controller owns the real palette. The local THEMES table
-- below is retained only as a safe fallback if the controller
-- is unavailable.
--------------------------------------------------

local THEME_PROFILE =
    "chat"


function ThemeColour(
    role,
    fallback
)

    local rc,
          value =
        CallPlugin(
            THEME_CONTROLLER_ID,
            "GetThemeColour",
            THEME_PROFILE,
            role
        )


    if rc ==
       error_code.eOK and
       type(
           value
       ) ==
       "string" and
       value ~=
       ""
    then

        return
            ColourNameToRGB(
                value
            )

    end


    return
        ColourNameToRGB(
            fallback
        )
end


THEMES = {

    -- New public default: the former green terminal/HUD theme.
    default = {
        WINDOW_BACKGROUND = "#000500",
        WINDOW_BORDER = "#285B28",

        HIGHLIGHT = "#B8FFAC",
        FACE = "#3F7A3F",
        INNERSHADOW = "#285328",
        OUTERSHADOW = "#102A10",

        BACK_FACE = "#285328",
        DETAIL = "#001000"
    },

    -- Classic: the former default grey theme.
    classic = {
        WINDOW_BACKGROUND = "#000000",
        WINDOW_BORDER = "#E8E8E8",

        HIGHLIGHT = "#FFFFFF",
        FACE = "#D4D0C8",
        INNERSHADOW = "#808080",
        OUTERSHADOW = "#404040",

        BACK_FACE = "#E8E8E8",
        DETAIL = "#000000"
    },

    amber = {
        WINDOW_BACKGROUND = "#050301",
        WINDOW_BORDER = "#6B3D18",

        HIGHLIGHT = "#FFCF8A",
        FACE = "#8A4A16",
        INNERSHADOW = "#5A2E0C",
        OUTERSHADOW = "#2C1606",

        BACK_FACE = "#5A2E0C",
        DETAIL = "#120A03"
    }
}

theme = {
    TITLE_HEIGHT = 20,
    SUBTITLE_HEIGHT = 17,
    TITLE_FONT_NAME = "Lucida Console",
    TITLE_FONT_SIZE = 10
}

function LoadTheme()

    local themeName =
        GetPluginVariable(
            THEME_CONTROLLER_ID,
            "effective_theme"
        )

    if not themeName or themeName == "" then
        themeName =
            GetPluginVariable(
                THEME_CONTROLLER_ID,
                "theme"
            )
    end

    themeName = tostring(themeName or "default"):lower()

    -- Compatibility with older Theme Controllers.
    if themeName == "green" then
        themeName = "default"
    end

    if not THEMES[themeName] then
        themeName = "default"
    end

    local T = THEMES[themeName]

    theme.WINDOW_BACKGROUND =
        ThemeColour("WINDOW_BACKGROUND", T.WINDOW_BACKGROUND)

    theme.WINDOW_BORDER =
        ThemeColour("WINDOW_BORDER", T.WINDOW_BORDER)

    theme.HIGHLIGHT =
        ThemeColour("HIGHLIGHT", T.HIGHLIGHT)

    theme.FACE =
        ThemeColour("FACE", T.FACE)

    theme.INNERSHADOW =
        ThemeColour("INNERSHADOW", T.INNERSHADOW)

    theme.OUTERSHADOW =
        ThemeColour("OUTERSHADOW", T.OUTERSHADOW)

    theme.BACK_FACE =
        ThemeColour("BACK_FACE", T.BACK_FACE)

    theme.DETAIL =
        ThemeColour("DETAIL", T.DETAIL)
end

LoadTheme()

function DrawThemed3DRect(Window, left, top, right, bottom)
    WindowRectOp(Window, miniwin.rect_fill, left, top, right, bottom, theme.FACE)
    WindowLine(Window, left, top, right, top, theme.HIGHLIGHT, miniwin.pen_solid + miniwin.pen_endcap_flat, 1)
    WindowLine(Window, left, top, left, bottom, theme.HIGHLIGHT, miniwin.pen_solid + miniwin.pen_endcap_flat, 1)
    WindowLine(Window, left, bottom-2, right, bottom-2, theme.INNERSHADOW, miniwin.pen_solid + miniwin.pen_endcap_flat, 1)
    WindowLine(Window, right-2, top, right-2, bottom-2, theme.INNERSHADOW, miniwin.pen_solid + miniwin.pen_endcap_flat, 1)
    WindowLine(Window, left, bottom-1, right, bottom-1, theme.OUTERSHADOW, miniwin.pen_solid + miniwin.pen_endcap_flat, 1)
    WindowLine(Window, right-1, top, right-1, bottom-1, theme.OUTERSHADOW, miniwin.pen_solid + miniwin.pen_endcap_flat, 1)
end

function DrawThemedResizeTag(Window, x1, y1, size)
    local x2, y2 = x1+size, y1+size
    DrawThemed3DRect(Window, x1, y1, x2, y2)
    local m = 2
    local n = 2
    while (x1+m+2 <= x2-3 and y1+n+1 <= y2-4) do
        WindowLine(Window, x1+m+1, y2-4, x2-3, y1+n, theme.HIGHLIGHT, miniwin.pen_solid, 1)
        WindowLine(Window, x1+m+2, y2-4, x2-3, y1+n+1, theme.INNERSHADOW, miniwin.pen_solid, 1)
        m = m+3
        n = n+3
    end
end

Win = GetPluginID()
font_height = ""
line_height = ""
windowinfo = ""
startx = ""
starty = ""
last_read_line = 0

function ResizeMoveCallback()
    posx, posy = WindowInfo (Win, 17), WindowInfo (Win, 18)
    if (WindowTextWidth(Win, "titlefont"..Win, "WWWCOMMUNICATION")+2*SCROLL_BAR_WIDTH <= WINDOW_WIDTH+posx-startx) then
        WINDOW_WIDTH = WINDOW_WIDTH+posx-startx
        startx = posx
    end
    if (3*SCROLL_BAR_WIDTH+10+line_height+theme.TITLE_HEIGHT <= WINDOW_HEIGHT+posy-starty) then
        WINDOW_HEIGHT = WINDOW_HEIGHT+posy-starty
        starty = posy
    end
    init(false)
end

function ResizeReleaseCallback()
    WINDOW_HEIGHT = theme.TITLE_HEIGHT+(line_height*(WINDOW_LINES-1))+3
    init(true)
end

function OnPluginInstall()
    LoadTheme()

    check (WindowCreate (Win, 0, 0, 1, 1, 0, 0, theme.WINDOW_BACKGROUND) )
    check (WindowFont(Win, "bodyfont"..Win, BODY_FONT_NAME, BODY_FONT_SIZE))
    check (WindowFont(Win, "titlefont"..Win, theme.TITLE_FONT_NAME, theme.TITLE_FONT_SIZE))
    font_height = WindowFontInfo (Win, "bodyfont"..Win, 1) - WindowFontInfo (Win, "bodyfont"..Win, 4) + 1
    line_height = font_height+1
    font_width = WindowTextWidth (Win, "bodyfont"..Win, "W")

    windowinfo = movewindow.install (Win, miniwin.pos_top_right, miniwin.create_absolute_location, true)

    if date_format == nil then
        date_format = "[%d %b %H:%M:%S] "
    end
    if WINDOW_WIDTH == nil then
        WINDOW_WIDTH = (font_width*80)+SCROLL_BAR_WIDTH
    end
    if WINDOW_HEIGHT == nil then
        WINDOW_HEIGHT = theme.TITLE_HEIGHT+(line_height*6)+2
    end
    init(true)
    OnPluginEnable ()
end

function init(firstTime)
    WINDOW_LINES = math.ceil((WINDOW_HEIGHT-theme.TITLE_HEIGHT)/line_height)
    WINDOW_COLUMNS = math.ceil((WINDOW_WIDTH-SCROLL_BAR_WIDTH)/font_width)

    if firstTime then
        WindowCreate(Win, windowinfo.window_left, windowinfo.window_top, WINDOW_WIDTH, WINDOW_HEIGHT, windowinfo.window_mode, windowinfo.window_flags, theme.WINDOW_BACKGROUND)

        WindowAddHotspot(Win, "textarea", 0, theme.TITLE_HEIGHT, WINDOW_WIDTH-SCROLL_BAR_WIDTH,0,
            "", "", "MouseDown", "CancelMouseDown", "MouseUp", "", miniwin.cursor_ibeam, 0)
        movewindow.add_drag_handler (Win, 0, 0, 0, theme.TITLE_HEIGHT)
        WindowScrollwheelHandler(Win, "textarea", "ScrollWheel")

        WindowAddHotspot(Win, "up", WINDOW_WIDTH-SCROLL_BAR_WIDTH, theme.TITLE_HEIGHT, 0, theme.TITLE_HEIGHT+SCROLL_BAR_WIDTH,
            "MouseOver", "CancelMouseOver", "MouseDown", "CancelMouseDown", "MouseUp", "", miniwin.cursor_hand, 0)
        WindowAddHotspot(Win, "down", WINDOW_WIDTH-SCROLL_BAR_WIDTH, WINDOW_HEIGHT-(2*SCROLL_BAR_WIDTH), 0, WINDOW_HEIGHT-SCROLL_BAR_WIDTH,
            "MouseOver", "CancelMouseOver", "MouseDown", "CancelMouseDown", "MouseUp", "", miniwin.cursor_hand, 0)

        WindowAddHotspot(Win, "resizer", WINDOW_WIDTH-SCROLL_BAR_WIDTH, WINDOW_HEIGHT-SCROLL_BAR_WIDTH, WINDOW_WIDTH, WINDOW_HEIGHT,
            "MouseOver", "CancelMouseOver", "MouseDown", "CancelMouseDown", "MouseUp", "", miniwin.cursor_nw_se_arrow, 0)
        WindowDragHandler(Win, "resizer", "ResizeMoveCallback", "ResizeReleaseCallback", 0)
    else
        WindowResize(Win, WINDOW_WIDTH, WINDOW_HEIGHT, theme.WINDOW_BACKGROUND)
        WindowMoveHotspot(Win, "textarea", 0, theme.TITLE_HEIGHT, WINDOW_WIDTH-SCROLL_BAR_WIDTH, 0)
        WindowMoveHotspot(Win, "up", WINDOW_WIDTH-SCROLL_BAR_WIDTH, theme.TITLE_HEIGHT, 0, theme.TITLE_HEIGHT+SCROLL_BAR_WIDTH)
        WindowMoveHotspot(Win, "down", WINDOW_WIDTH-SCROLL_BAR_WIDTH, WINDOW_HEIGHT-(2*SCROLL_BAR_WIDTH), 0, WINDOW_HEIGHT-SCROLL_BAR_WIDTH)
        WindowMoveHotspot(Win, "resizer", WINDOW_WIDTH-SCROLL_BAR_WIDTH, WINDOW_HEIGHT-SCROLL_BAR_WIDTH, WINDOW_WIDTH, 0)
    end
    WindowShow(Win, true)

    if (firstTime == true) then
        lines = {}
        for _,styles in ipairs(rawlines) do
            fillBuffer(styles)
        end
    end

    lineStart = math.max(1, #lines-WINDOW_LINES+2)
    lineEnd = math.max(1, #lines)
    refresh()
end

function OnPluginClose ()
    if GetPluginInfo (GetPluginID(), 17) then
        OnPluginDisable()
    end
end

function OnPluginEnable ()
    LoadTheme()
    refresh()
    WindowShow (Win, true)
end

function OnPluginSaveState ()
    SetVariable ("enabled", tostring (GetPluginInfo (GetPluginID(), 17)))
    movewindow.save_state (Win)
    SetVariable ("echo", tostring (echo))
    SetVariable ("timestamp", tostring (timestamp))
    SetVariable("date_format", date_format)
    SetVariable("WINDOW_WIDTH", WINDOW_WIDTH)
    SetVariable("WINDOW_HEIGHT", WINDOW_HEIGHT)
end

function OnPluginDisable ()
    WindowShow( Win, false )
end

function Display_Line (line, styles)
  local left    = TEXT_INSET
  local top     = theme.TITLE_HEIGHT + (line * line_height) + 1
  local bottom  = top + line_height
  local font    = "bodyfont" .. Win

  if backfill then
      WindowRectOp (Win, miniwin.rect_fill, 1, top, WINDOW_WIDTH - SCROLL_BAR_WIDTH, bottom, theme.OUTERSHADOW)
  end

  if (styles) then
    for _, style in ipairs (styles) do
      local width = WindowTextWidth (Win, font, style.text)
      local right = left + width
      WindowRectOp (Win, miniwin.rect_fill, left, top, right, bottom, style.backcolour)
      WindowText (Win, font, style.text, left, top, 0, 0, style.textcolour)
      left = left + width
    end
  end
end

function writeLines()
    for count = lineStart, lineEnd do
        Display_Line( count-lineStart, lines[count][1], false )
    end
    Redraw()
end

function refresh()
    WindowRectOp(Win, miniwin.rect_fill, 0, 0, WINDOW_WIDTH, WINDOW_HEIGHT, theme.WINDOW_BACKGROUND)
    drawStuff()
end

barPos = ""
barSize = ""
totalSteps = ""

function drawStuff()
    WindowRectOp (Win, miniwin.rect_frame, 0, 0, 0, 0, theme.WINDOW_BORDER)
    DrawThemed3DRect(Win, 0, 0, WINDOW_WIDTH, theme.TITLE_HEIGHT)
    WindowText(Win, "titlefont"..Win, "COMMUNICATION", ((WINDOW_WIDTH)-(7.5*line_height))/2, (theme.TITLE_HEIGHT-line_height)/2, WINDOW_WIDTH, theme.TITLE_HEIGHT, theme.DETAIL, false)

    if #lines >= 1 then
        writeLines()
    end

    WindowRectOp (Win, miniwin.rect_fill, WINDOW_WIDTH-SCROLL_BAR_WIDTH, theme.TITLE_HEIGHT, WINDOW_WIDTH, WINDOW_HEIGHT, theme.BACK_FACE)
    WindowRectOp (Win, miniwin.rect_frame, WINDOW_WIDTH-SCROLL_BAR_WIDTH+1, SCROLL_BAR_WIDTH+theme.TITLE_HEIGHT+1, WINDOW_WIDTH-1, WINDOW_HEIGHT-(2*SCROLL_BAR_WIDTH)-1, theme.DETAIL)
    DrawThemed3DRect(Win, WINDOW_WIDTH-SCROLL_BAR_WIDTH, theme.TITLE_HEIGHT, WINDOW_WIDTH, theme.TITLE_HEIGHT+SCROLL_BAR_WIDTH)
    DrawThemed3DRect(Win, WINDOW_WIDTH-SCROLL_BAR_WIDTH, WINDOW_HEIGHT-(SCROLL_BAR_WIDTH*2), WINDOW_WIDTH, WINDOW_HEIGHT-SCROLL_BAR_WIDTH)

    points = string.format ("%i,%i,%i,%i,%i,%i", (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+2, theme.TITLE_HEIGHT+8,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+6, theme.TITLE_HEIGHT+4,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+10, theme.TITLE_HEIGHT+8)
    WindowPolygon (Win, points, theme.DETAIL, miniwin.pen_solid, 1, theme.DETAIL, miniwin.brush_solid, true, false)

    points = string.format ("%i,%i,%i,%i,%i,%i", (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+2, (WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-10,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+6, (WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-6, (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+10,(WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-10)
    WindowPolygon (Win, points, theme.DETAIL, miniwin.pen_solid, 1, theme.DETAIL, miniwin.brush_solid, true, false)

    totalSteps = #lines
    if (totalSteps <= WINDOW_LINES-1) then totalSteps = 1 end
    SCROLL_BAR_HEIGHT = (WINDOW_HEIGHT-(3*SCROLL_BAR_WIDTH)-theme.TITLE_HEIGHT)
    if (not dragscrolling) then
        stepNum = lineStart-1
        barPos = SCROLL_BAR_WIDTH +theme.TITLE_HEIGHT+ ((SCROLL_BAR_HEIGHT/totalSteps) * stepNum)
        barSize = (SCROLL_BAR_HEIGHT/math.max(WINDOW_LINES-1,totalSteps)) * (WINDOW_LINES-1)
        if barSize < 10 then
            barSize = 10
        end
        if barPos+barSize > SCROLL_BAR_WIDTH+theme.TITLE_HEIGHT+SCROLL_BAR_HEIGHT then
            barPos = SCROLL_BAR_WIDTH+theme.TITLE_HEIGHT+SCROLL_BAR_HEIGHT - barSize
        end
        WindowAddHotspot(Win, "scroller", (WINDOW_WIDTH-SCROLL_BAR_WIDTH), barPos, WINDOW_WIDTH, barPos+barSize,
                        "MouseOver", "CancelMouseOver", "MouseDown", "CancelMouseDown", "MouseUp", "", miniwin.cursor_hand, 0)
        WindowDragHandler(Win, "scroller", "ScrollerMoveCallback", "ScrollerReleaseCallback", 0)
    end
    DrawThemed3DRect(Win, WINDOW_WIDTH-SCROLL_BAR_WIDTH, barPos, WINDOW_WIDTH, barPos+barSize)
    DrawThemedResizeTag(Win, WINDOW_WIDTH-SCROLL_BAR_WIDTH, WINDOW_HEIGHT-SCROLL_BAR_WIDTH, SCROLL_BAR_WIDTH)
    Redraw()
end

function ScrollWheel (flags, hotspot_id)
    local steps = 3   -- lines scrolled per wheel notch; raise/lower to taste
    if bit.band(flags, 0x100) ~= 0 then
        -- Wheel down: show later (newer) lines; ignore modifier bits.
        for i = 1, steps do
            if lineEnd < #lines then
                lineStart = lineStart + 1
                lineEnd = lineEnd + 1
            end
        end
    else
        -- Wheel up: show earlier (older) lines.
        for i = 1, steps do
            if lineStart > 1 then
                lineStart = lineStart - 1
                lineEnd = lineEnd - 1
            end
        end
    end
    refresh()
end

function ScrollerMoveCallback(flags, hotspot_id)
    mouseposy = WindowInfo(Win, 18)
    windowtop = WindowInfo(Win, 2)
    barPos = math.max(mouseposy-windowtop+clickdelta, SCROLL_BAR_WIDTH+theme.TITLE_HEIGHT)
    if barPos > WINDOW_HEIGHT-(SCROLL_BAR_WIDTH*2)-barSize then
        barPos = WINDOW_HEIGHT-(SCROLL_BAR_WIDTH*2)-barSize
        lineStart = math.max(1,#lines-WINDOW_LINES+2)
        lineEnd = #lines
    else
        lineStart = math.floor((barPos-SCROLL_BAR_WIDTH-theme.TITLE_HEIGHT)/(SCROLL_BAR_HEIGHT/totalSteps)+1)
        lineEnd = math.min(lineStart + WINDOW_LINES-2, #lines)
    end
    refresh()
end

function ScrollerReleaseCallback(flags, hotspot_id)
    dragscrolling = false
    refresh()
end

function fillBuffer(rawstyles)
    local avail = 0
    local line_styles
    local beginning = true
    local styles = copytable.deep (rawstyles)
    local remove = table.remove
    local insert = table.insert
    while #styles > 0 do
        if avail <= 0 then
            if #lines >= MAX_LINES then
                remove (lines, 1)
            end
            avail = WINDOW_WIDTH - (TEXT_INSET * 2) - 9
            line_styles = {}
            add_line( line_styles, beginning )
            beginning = false
        end

        local style = remove (styles, 1)
        local width = WindowTextWidth (Win, "bodyfont"..Win, style.text)

        if width <= avail then
            insert (line_styles, style)
            avail = avail - width
        else
            local col = style.length - 1
            local split_col
            while col > 1 do
                width = WindowTextWidth (Win, "bodyfont"..Win, style.text:sub (1, col))
                if width <= avail then
                    if not split_col then
                        split_col = col
                    end
                    if style.text:sub (col, col) == " " then
                        split_col = col
                        break
                    end
                end
                col = col - 1
            end

            if split_col then
                insert (line_styles, style)
                local style_copy = copytable.shallow (style)
                style.text = style.text:sub (1, split_col)
                style.length = split_col
                style_copy.text = style_copy.text:sub (split_col + 1)
                style_copy.length = #style_copy.text
                insert (styles, 1, style_copy)
            elseif next (line_styles) == nil then
                insert (line_styles, style)
            else
                insert (styles, 1, style)
            end
            avail = 0
        end
    end
end

local function display_internal (styles_text, allow_echo)
    assert (loadstring ("styles = " .. styles_text)) ()

    if allow_echo and echo then
        for _, v in ipairs (styles) do
            ColourTell (RGBColourToName (v.textcolour),RGBColourToName (v.backcolour),v.text)
        end
        Note ("")
    end

    if timestamp then
        local tstamp = os.date (date_format)
        table.insert (styles, 1, {
          text = tstamp,
          textcolour  = ColourNameToRGB (TIMESTAMP_TEXT_COLOUR),
          backcolour = ColourNameToRGB (TIMESTAMP_BACK_COLOUR),
          length = string.len (tstamp),
          style = 0,
        } )
    end

    if #rawlines >= MAX_LINES then
        table.remove(rawlines, 1)
    end
    table.insert(rawlines, styles)

    fillBuffer(styles)
    refresh( )
end

function display (styles_text)
    display_internal(styles_text, true)
end

-- Route-safe API: always capture into the miniwindow without echoing the
-- caught server line back into the main output window.
function display_noecho (styles_text)
    display_internal(styles_text, false)
end

function add_line ( line, is_beginning_of_message )
    table.insert (lines, {line, false} )
    lines[#lines][2] = is_beginning_of_message

    -- Always follow the newest message.
    -- This keeps the chat window at the bottom when new messages arrive.
    if #lines >= WINDOW_LINES then
        lineStart = #lines - WINDOW_LINES + 2
        lineEnd = #lines
    else
        lineStart = 1
        lineEnd = #lines
    end
end

keepscrolling = false
require "wait"

function scrollbar(calledBy)
    wait.make (function()
        while keepscrolling == true do
            if calledBy == "up" then
                if (lineStart > 1) then
                    lineStart = lineStart - 1
                    lineEnd = lineEnd - 1
                    WindowRectOp(Win, miniwin.rect_draw_edge, (WINDOW_WIDTH-SCROLL_BAR_WIDTH), theme.TITLE_HEIGHT, 0, theme.TITLE_HEIGHT+SCROLL_BAR_WIDTH,
                        miniwin.rect_edge_sunken,  miniwin.rect_edge_at_all + miniwin.rect_option_fill_middle)
                    points = string.format ("%i,%i,%i,%i,%i,%i", (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+3, theme.TITLE_HEIGHT+9,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+7, theme.TITLE_HEIGHT+5,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+11, theme.TITLE_HEIGHT+9)
                    WindowPolygon (Win, points, theme.DETAIL, miniwin.pen_solid, 1, theme.DETAIL, miniwin.brush_solid, true, false)
                else
                    keepscrolling = false
                end
            elseif calledBy == "down" then
                if (lineEnd < #lines) then
                    lineStart = lineStart + 1
                    lineEnd = lineEnd + 1
                    WindowRectOp(Win, miniwin.rect_draw_edge, (WINDOW_WIDTH-SCROLL_BAR_WIDTH), WINDOW_HEIGHT-(SCROLL_BAR_WIDTH*2), 0, WINDOW_HEIGHT-SCROLL_BAR_WIDTH-1,
                        miniwin.rect_edge_sunken,  miniwin.rect_edge_at_all + miniwin.rect_option_fill_middle)
                    points = string.format ("%i,%i,%i,%i,%i,%i", (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+3, (WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-11,(WINDOW_WIDTH-SCROLL_BAR_WIDTH)+7, (WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-7, (WINDOW_WIDTH-SCROLL_BAR_WIDTH)+11,(WINDOW_HEIGHT-SCROLL_BAR_WIDTH)-11)
                    WindowPolygon (Win, points, theme.DETAIL, miniwin.pen_solid, 1, theme.DETAIL, miniwin.brush_solid, true, false)
                else
                    keepscrolling = false
                end
            end
            wait.time(0.1)
            refresh()
        end
    end)
end

function GetLineText (styles)
  local t = {}
  for _, style in ipairs (styles) do
    table.insert (t, style.text)
  end
  return table.concat (t)
end

function GetAllBufferedMessages()
    local t = {}
    for _,styles in ipairs(rawlines) do
      table.insert (t, GetLineText (styles))
    end
    SetClipboard(table.concat(t,"\r\n"))
end

function GetBufferedMessage(xpos, ypos)
    windowline = math.floor(((ypos-theme.TITLE_HEIGHT)/line_height)+1)-1
    text = ""
    if (#lines > windowline) then
        local line = windowline+lineStart
        while lines[line][2] ~= true and line > 1 do
            line = line - 1
        end
        local styles = copytable.deep(lines[line][1])
        if (line-lineStart+1 > 0) then
            Display_Line (line-lineStart, styles, true)
        end
        text = GetLineText (styles)
        line = line + 1
        while line <= #lines and lines[line][2] ~= true do
            local styles = copytable.deep(lines[line][1])
            if (line-lineStart+1 > 0 and line-lineStart < WINDOW_LINES) then
                Display_Line (line-lineStart, styles, true)
            end
            text = text.. GetLineText (styles)
            line = line + 1
        end
        SetClipboard(text)
    end
    Redraw()
end

function MouseOver(flags, hotspot_id)
    keepscrolling = false
end

function CancelMouseOver(flags, hotspot_id)
    keepscrolling = false
end

function MouseDown(flags, hotspot_id)
    if (hotspot_id == "resizer") then
        startx, starty = WindowInfo (Win, 17), WindowInfo (Win, 18)
    elseif (hotspot_id == "scroller") then
        clickdelta = WindowHotspotInfo(Win, "scroller", 2)-WindowInfo (Win, 15)
        dragscrolling = true
    elseif (hotspot_id == "textarea" and flags == miniwin.hotspot_got_lh_mouse) then
        GetBufferedMessage(WindowInfo(Win, 14), WindowInfo(Win,15))
    else
        keepscrolling = true
        scrollbar(hotspot_id)
    end
end

function CancelMouseDown(flags, hotspot_id)
    keepscrolling = false
    refresh()
end

function MouseUp(flags, hotspot_id)
    if (hotspot_id == "textarea" and flags == miniwin.hotspot_got_rh_mouse) then
        right_click_menu()
    else
        refresh()
    end
    keepscrolling = false
end

function chat_echo (name, line, wildcards)
    if wildcards [1] == "" then
      echo = not echo
    else
      echo = wildcards [1]:lower () == " on"
    end
    if echo then
        ColourNote ("yellow", "", "Echoing chats in main window ENABLED.")
    else
        ColourNote ("yellow", "", "Echoing chats in main window DISABLED.")
    end
end

function chat_sapi (name, line, wildcards)
  if last_read_line >= #lines then
    CallPlugin ("463242566069ebfd1b379ec1", "say", "No more.")
    return
  end
  last_read_line = last_read_line + 1
  local styles = lines [last_read_line] [1]
  local full_line = ""
  while true do
    if last_read_line > #lines then break end
    if not styles then break end
    for _, style in ipairs (styles) do
      full_line = full_line .. style.text
    end
    if last_read_line >= #lines or lines [last_read_line + 1] [2] then
      break
    end
    last_read_line = last_read_line + 1
    styles = lines [last_read_line] [1]
  end
  print ("Speaking '" .. full_line .. "'")
  CallPlugin ("463242566069ebfd1b379ec1", "say", full_line)
end

function chat_timestamps (name, line, wildcards)
    if wildcards [1] == "" then
      timestamp = not timestamp
    else
      timestamp = wildcards [1]:lower () == " on"
    end
    if timestamp then
        ColourNote ("yellow", "", "Time-stamping chats in main window ENABLED.")
    else
        ColourNote ("yellow", "", "Time-stampin chats in main window DISABLED.")
    end
end

function chat_show (name, line, wildcards)
   WindowShow( Win, true )
   ColourNote ("yellow", "", "Chats window now shown. Type 'chats hide' to hide it.")
end

function chat_hide (name, line, wildcards)
   WindowShow( Win, false )
   ColourNote ("yellow", "", "Chats window now hidden. Type 'chats show' to see it again.")
end

function right_click_menu ()
    menustring ="Copy All To Clipboard|Change Font|Turn Echo "
    if echo then
        menustring = menustring .. "Off"
    else
        menustring = menustring .. "On"
    end
    menustring = menustring.."|>Timestamp|No Timestamps|30 Aug 13:29:49|30 Aug 01:20:12PM|13:29:08|1:22:06 PM"
    result = WindowMenu (Win, WindowInfo (Win, 14), WindowInfo (Win, 15), menustring)
    if result == "Copy All To Clipboard" then
        GetAllBufferedMessages()
        ColourNote ("yellow", "", "All buffered messages copied to clipboard.")
    elseif result == "Change Font" then
        wanted_font = utils.fontpicker (BODY_FONT_NAME, BODY_FONT_SIZE)
        if wanted_font then
            BODY_FONT_NAME = wanted_font.name
            BODY_FONT_SIZE = wanted_font.size
            SetVariable ("bodyfont", BODY_FONT_NAME)
            SetVariable ("font_size", BODY_FONT_SIZE)
            OnPluginInstall()
        end
    elseif result == "Turn Echo Off" then
        echo = false
        ColourNote ("yellow", "", "Echoing chats in main window DISABLED.")
    elseif result == "Turn Echo On" then
        echo = true
        ColourNote ("yellow", "", "Echoing chats in main window ENABLED.")
    elseif result == "No Timestamps" then
        timestamp = false
        ColourNote ("yellow", "", "Timestamps in communication window DISABLED.")
    elseif result == "30 Aug 13:29:49" then
        timestamp = true
        date_format = "[%d %b %H:%M:%S] "
        ColourNote ("yellow", "", "Time-stamping chats in main window ENABLED using format like '30 Aug 13:29:49'.")
    elseif result == "30 Aug 01:20:12PM" then
        timestamp = true
        date_format = "[%d %b %I:%M:%S%p] "
        ColourNote ("yellow", "", "Time-stamping chats in main window ENABLED using format like '30 Aug 01:20:12PM'.")
    elseif result == "13:29:08" then
        timestamp = true
        date_format = "[%H:%M:%S] "
        ColourNote ("yellow", "", "Time-stamping chats in main window ENABLED using format like '13:29:08'.")
    elseif result == "1:22:06 PM" then
        timestamp = true
        date_format = "[%I:%M:%S%p] "
        ColourNote ("yellow", "", "Time-stamping chats in main window ENABLED using format like '1:22:06 PM'.")
    end
end

--------------------------------------------------
-- THEME BROADCAST
--------------------------------------------------

function OnPluginBroadcast(
    msg,
    id,
    name,
    text
)

    if msg ~= THEME_BROADCAST_MESSAGE then
        return
    end

    if id ~= THEME_CONTROLLER_ID then
        return
    end

    LoadTheme()

    --------------------------------------------------
    -- Repaint only the miniwindow chrome/background.
    -- The chat message style runs keep their original
    -- MUD textcolour/backcolour values.
    --------------------------------------------------

    refresh()
end



--------------------------------------------------
-- SQUARESOFT SUITE INTEGRATION
--
-- This does NOT change chat capture.  The original
-- plugin still receives style runs through display().
--------------------------------------------------

function GetManagedLayoutWindows()
    return Win
end

local function RebuildChatFontMetrics()
    check (WindowFont(Win, "bodyfont"..Win, BODY_FONT_NAME, BODY_FONT_SIZE))
    check (WindowFont(Win, "titlefont"..Win, theme.TITLE_FONT_NAME, theme.TITLE_FONT_SIZE))

    font_height =
        WindowFontInfo(Win, "bodyfont"..Win, 1) -
        WindowFontInfo(Win, "bodyfont"..Win, 4) + 1

    line_height = font_height + 1
    font_width = WindowTextWidth(Win, "bodyfont"..Win, "W")
end

function ApplyManagedLayout(spec)
    local windowName, sx, sy, sw, sh =
        tostring(spec or ""):match(
            "^([^\t]+)\t(-?%d+)\t(-?%d+)\t(%d+)\t(%d+)$"
        )

    if windowName ~= Win then
        return "wrong window"
    end

    local x = tonumber(sx)
    local y = tonumber(sy)
    local w = tonumber(sw)
    local h = tonumber(sh)

    if not x or not y or not w or not h then
        return "invalid layout"
    end

    local minimumWidth =
        WindowTextWidth(
            Win,
            "titlefont"..Win,
            "WWWCOMMUNICATION"
        ) +
        2 * SCROLL_BAR_WIDTH

    local minimumHeight =
        3 * SCROLL_BAR_WIDTH +
        10 +
        line_height +
        theme.TITLE_HEIGHT

    WINDOW_WIDTH = math.max(minimumWidth, math.floor(w))
    WINDOW_HEIGHT = math.max(minimumHeight, math.floor(h))

    if type(windowinfo) ~= "table" then
        return "window not ready"
    end

    windowinfo.window_left = x
    windowinfo.window_top = y

    init(false)

    WindowPosition(
        Win,
        x,
        y,
        windowinfo.window_mode,
        windowinfo.window_flags
    )

    movewindow.save_state(Win)

    SetVariable("WINDOW_WIDTH", tostring(WINDOW_WIDTH))
    SetVariable("WINDOW_HEIGHT", tostring(WINDOW_HEIGHT))

    pcall(SaveState)
    Redraw()

    return "ok"
end

function ApplyManagedTypography(scaleText)
    local scale = tonumber(scaleText) or 1.0

    if scale < 0.85 then scale = 0.85 end
    if scale > 1.35 then scale = 1.35 end

    BODY_FONT_SIZE =
        math.max(
            8,
            math.min(
                15,
                math.floor(BASE_BODY_FONT_SIZE * scale + 0.5)
            )
        )

    theme.TITLE_FONT_SIZE =
        math.max(
            8,
            math.min(
                14,
                math.floor(BASE_TITLE_FONT_SIZE * scale + 0.5)
            )
        )

    SetVariable("font_size", tostring(BODY_FONT_SIZE))

    RebuildChatFontMetrics()
    init(false)

    pcall(SaveState)
    return "ok"
end

]]>
</script>
</muclient>